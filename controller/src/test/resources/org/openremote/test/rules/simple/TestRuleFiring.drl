package org.openremote.test.rules;

import org.openremote.controller.sensor.*;
import org.openremote.controller.rules.*

rule "TestRuleFiring"
when
	$e: SensorState(sensorName == RuleExecutionLoggerTest.TEST_SENSOR_NAME)
then
// Do Nothing
end

