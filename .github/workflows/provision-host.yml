# This workflow handles creation of AWS EC2 hosts under the OpenRemote hosted zone
name: Provision Host

on:
  workflow_dispatch:
    inputs:
      HOST:
        description: 'Host to provision'
        required: true
      INSTANCE_TYPE:
        description: 'EC2 instance type (only change if you know what you are doing)'
        default: t4g.small
        options:
          - t4g.small
          - t4g.medium
          - t4g.large
          - m6g.medium
          - m6g.large
        required: true

  jobs:

    provision:
      name: Provision
      runs-on: ubuntu-latest

      steps:
        -run: echo ""
