persistenceUnitName=CCS-MySQL
persistenceCreateSchema=true

proxy.hostname=localhost
proxy.timeout=10000
proxy.port=10000
proxy.clientPortRange=30000-30010
proxy.useSSL=true
proxy.keystore=/keystore.ks
proxy.keystorePassword=storepass
