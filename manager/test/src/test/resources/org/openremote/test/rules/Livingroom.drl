package org.openremote.test.rules;

import org.openremote.model.*;
import org.openremote.manager.server.asset.*;

global java.util.logging.Logger LOG;

rule "Living Room All"
when
  $e: AssetUpdate(entityName == "Livingroom")
then
  return;
end

rule "Living Room Thermostat"
when
  $e: AssetUpdate(entityName == "Livingroom Thermostat")
then
  return;
end