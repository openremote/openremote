evaluationDependsOnChildren()

task prepareImage(type: Copy) {
    dependsOn resolveTask(":manager:server:installDist"), resolveTask(":manager:client:installDist")
    from "Dockerfile"
    into "${project.buildDir}/install"
}

task clean() {
    delete project.buildDir
}