<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
             xmlns:g="urn:import:com.google.gwt.user.client.ui"
             xmlns:w="urn:import:org.openremote.manager.client.widget">

    <ui:with field="managerMessages" type="org.openremote.manager.client.i18n.ManagerMessages"/>
    <ui:with field="widgetStyle" type="org.openremote.manager.client.style.WidgetStyle"/>

    <ui:style field="style" type="org.openremote.manager.client.admin.syslog.QuickSyslogImpl.Style">
        .popup {
            z-index: 10;
        }

        .panel {
            width: 36em;
            height: 25em;
        }

        .header {
            font-weight: 700;
            padding: 0.4em;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .filterForm {
        }

        .filterFormGroup {
            margin: 0 !important;
        }

        .filterFormLabel {
            width: inherit !important;
        }

        .filterFormField {
            min-width: inherit !important;
        }

        .logPanel {
            overflow: auto;
        }

        .logItem {
            font-size: x-small;
        }

        .logItemLevelCategory {
            font-weight: bold;
        }

    </ui:style>

    <w:PopupPanel glassEnabled="false" styleName="{style.popup} {widgetStyle.PopupPanel}">
        <g:HTMLPanel styleName="layout vertical {style.panel}">

            <div class="layout horizontal center {style.header} {widgetStyle.PopupPanelHeader}">

                <w:Form addStyleNames="flex {style.filterForm}">
                    <w:FormGroup addStyleNames="{style.filterFormGroup}">
                        <w:label>
                            <w:FormLabel addStyleNames="{style.filterFormLabel}"
                                         text="{managerMessages.filterLevel}"/>
                        </w:label>
                        <w:field>
                            <w:FormField addStyleNames="{style.filterFormField}">
                                <w:FormValueListBox ui:field="levelListBox"/>
                            </w:FormField>
                        </w:field>
                        <w:actions>
                            <w:FormGroupActions>
                                <w:FormButton ui:field="pauseButton"
                                              icon="pause"
                                              text="{managerMessages.pauseLog}"/>
                                <w:FormButton ui:field="continueButton"
                                              icon="play"
                                              visible="false"
                                              text="{managerMessages.continueLog}"/>
                                <w:FormButton ui:field="clearButton"
                                              icon="eraser"
                                              text="{managerMessages.clearLog}"/>
                            </w:FormGroupActions>
                        </w:actions>
                    </w:FormGroup>
                </w:Form>

            </div>

            <div class="flex layout vertical {widgetStyle.PopupPanelContent}">
                <g:FlowPanel ui:field="logPanel"
                             addStyleNames="flex layout vertical {style.logPanel} {widgetStyle.FormList}"/>
            </div>

        </g:HTMLPanel>
    </w:PopupPanel>

</ui:UiBinder>
