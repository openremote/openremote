apply plugin: "java"

dependencies {

    compile project(":container")
    compile project(":manager:shared")

    compile files("libs/orlibrary-0.3.1-SNAPSHOT.jar")
    compile files("libs/orentities-1.3.1-SNAPSHOT.jar")
}
