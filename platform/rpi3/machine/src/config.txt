# Enable serial port
enable_uart=1

# Disable camera
start_x=0

# Use minimum of GPU memory
gpu_mem=16

# Use HDMI mode even if no HDMI monitor is detected
hdmi_force_hotplug=1

# Enable audio
dtparam=audio=on
