package org.openremote.test.rules;

import org.openremote.agent.sensor.*;
import org.openremote.agent.rules.*

rule "TestRuleFiring"
when
	$e: SensorState(sensorName == RuleExecutionLoggerTest.TEST_SENSOR_NAME)
then
// Do Nothing
end

