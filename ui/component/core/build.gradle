buildDir = "dist"

task clean() {
    doLast {
        delete "dist"
    }
}

npmBuild.dependsOn resolveTask(":ui:component:model:build"), resolveTask(":ui:component:rest:build")

task build() {
    dependsOn npmBuild
}

task installDist() {
    dependsOn build
}