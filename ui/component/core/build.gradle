buildDir = "dist"

task clean() {
    doLast {
        delete "dist"
        delete "lib"
    }
}

task prepareUi() {
    dependsOn clean, npmPrepare
}

task publishUi() {
    dependsOn clean, npmPublish
}
