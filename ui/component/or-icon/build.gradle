buildDir = "dist"

task clean() {
    doLast {
        delete "dist"
    }
}

task generate(type: Exec){
    commandLine npmCommand("npm"), "run-script", "generate"
}

task build() {
    dependsOn npmBuild
}