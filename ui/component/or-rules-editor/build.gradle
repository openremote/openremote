buildDir = "dist"

task clean() {
    doLast {
        delete "dist"
    }
}

task build() {
    dependsOn npmBuild, resolveTask(":ui:component:or-select:build"), resolveTask(":ui:component:or-input:build")
}

task installDist() {
    dependsOn build
}