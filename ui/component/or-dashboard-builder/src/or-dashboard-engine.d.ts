import { GridStackEngine, GridStackNode, GridStackMoveOpts } from 'gridstack';
export declare class OrDashboardEngine extends GridStackEngine {
    moveNode(node: GridStackNode, o: GridStackMoveOpts): boolean;
}
