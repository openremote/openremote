module.exports = {
    // URLs
    OpenRemote_DEMO_URL: 'https://demo.openremote.io/manager/?realm=smartcity',
    OpenRemote_Local_URL: 'http://localhost:8080/manager/',
    OpenRemote_URL: 'https://staging.demo.openremote.io/manager/?realm=master',

    // setup
    // globalSetup: require.resolve('./setup'),
    // use: {
    //   // Tell all tests to load signed-in state from 'storageState.json'.
    //   storageState: 'storageState.json'
    // }
}