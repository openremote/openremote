//buildDir = "dist"
//npmBuild.dependsOn(resolveTask(":ui:component:core:build"))
//
//task build {
//    dependsOn npmBuild
//}
//
//task installDist(type: Copy) {
//    dependsOn build
//    from(buildDir) {
//        exclude "gwt", "build.gradle", "package.json", "yarn.lock", ".bowerrc"
//    }
//    into "${resolveProject(':deployment').buildDir}/manager/app/${projectDir.name}"
//}
