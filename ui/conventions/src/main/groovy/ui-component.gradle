plugins {
    id "base"
}

tasks.named("clean") {
    delete "dist", "lib" // TODO: remove lib when tsc -b --clean is configured correctly
    // Clear the sources generated by the TypeScript compiler
//    dependsOn getYarnInstallTask()
//    commandLine npmCommand("yarn"), "run", "clean"
}

// Every UI project gets an "npmTest" task by default
//tasks.register("npmTest", Exec) {
//    group = "verification"
//    executable = "npm"
//    args = ["test"]
//}
